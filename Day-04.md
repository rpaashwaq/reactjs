```markdown
Day - 04
```
```markdown

useRef
- Its is a React Hook that lets you reference a value from DOM element 
- refs are used to getting the reference to a DOM node or an instance of a component
  <input type="text" ref={addressRef} />
  addressRef.current.value="Mumbai";

useEffect
- A function that runs code at speectice time, based on dependencies provided.
  It will automatically trigger based on state changed
	useEffect(()=>{
	}, [dependencies]);
	
```
